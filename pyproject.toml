[tool.ruff]
# Exclure les dossiers de migration
exclude = [
    "jeu_trains/core/migrations",
    "jeu_trains/mongo_migrations",
    "jeu_trains/jeu_trains/settings.py",
]
line-length = 300

[tool.ruff.lint]
# Activer les règles de base (Pyflakes, Pycodestyle)
select = ["E", "F", "I"]
