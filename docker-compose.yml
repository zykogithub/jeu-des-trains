version: "3.8"

services:
  db:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jeu_trains_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
